just run simply with

./Run.sh BOOLEAN UNSIGNEDNUM

Boolean decides if it uses the tab character or 4 spaces for indenting the function and unsigned number decides how many numbers the outputted function will check through to, range is (0, x-1).
